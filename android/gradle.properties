org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Keep this project on an ASCII path. The Android native toolchain (cmake /
# ninja) cannot handle non-ASCII directory names on Windows, so the earlier
# ...\সহজপাঠ\ location could not produce an APK. The Bengali name lives in the
# app label and MaterialApp title instead.
